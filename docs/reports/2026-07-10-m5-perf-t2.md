# M5 Acceptance Report: Performance Benchmark + T2 Full Soak Test

Date: 2026-07-10.

## Performance Benchmark (T1-200, Full Pipeline L1+L2+L3)

- Before fix: 22,960 lines/sec (bottleneck = constructing `Intl.DateTimeFormat` per line)
- After fix (per-timezone formatter caching, commit `861b3c1`): **104,802 lines/sec** — 2.1× the spec target of ≥50k, ~100× real-time tailing requirement.
- tsx and compiled artifacts (esbuild) exhibit identical speeds; bottleneck was genuine computation, identified and eliminated.

## T2 Full Soak Test (Full Self-Collected Corpus)

| Metric | Value |
| --- | --- |
| Files | 6,210 (104GB) |
| Non-empty lines | 386,247,153 |
| Time elapsed | 3,636s (≈1h), 106,222 lines/sec |
| Matches constructed | 3,060 matches + 3,150 shuffles |
| Errors / Diagnostics / Dropped lines | **0 / 0 / 0** |

## Subproject 1 Sign-Off Verdict

M1 (SNR 386M lines zero failures), M2 (Segmenter + six scenario contracts), M3 (Builder + golden assertions), M4 (Differential: Level-1 599/600, unresolved diffs = 0), and M5 (Performance 2× target + soak test zero anomalies) all achieved. **`@gladlog/parser` and `@gladlog/parser-compat` are ready for Subprojects 2/4 consumption.**

Backlog items (non-blocking): L1 explicit stripping of `\r` (adjudication #15); typing unknown event families on demand; `classMetadata` injection point awaiting Subproject 5 data.
